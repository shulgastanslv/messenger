<UserControl x:Class="Client.Views.EditUserNameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Width="320" Height="250"
          Background="{StaticResource EditUserNameBackgroundColorBrush}">

        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBox Margin="0, 30, 0, 0"
                     Text="{Binding UserName, 
                            UpdateSourceTrigger=PropertyChanged}"
                     Tag="Enter new name" />

            <Button Content="Save" Margin="0, 10, 0, 10"
                    Command="{Binding SaveCommand}" />

            <Button Style="{StaticResource BackButtonStyle}"
                    Content="Cancel"
                    Command="{Binding NavigateToEditProfileCommand}" />

        </StackPanel>


    </Grid>
</UserControl>