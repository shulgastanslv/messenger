<Page x:Class="Client.Views.Pages.ChatView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views.Pages"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:viewModels="clr-namespace:Client.ViewModels"
      xmlns:models="clr-namespace:Client.Models"
      mc:Ignorable="d" 
      d:DesignHeight="620" d:DesignWidth="1085"
      Title="ChatView">

    <Grid>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
                <RowDefinition Height="55"/>
            </Grid.RowDefinitions>


            <Border Background="{StaticResource HeaderChatColorBrush}">

                <Grid HorizontalAlignment="Stretch" Margin="0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <StackPanel  VerticalAlignment="Center" Grid.Column="1">
                        <Label Content="{Binding CurrentContact.UserName}" 
                               Foreground="White"
                               HorizontalContentAlignment="Left"
                               FontSize="14"
                               Padding="0, 5, 5, 5"
                               Width="Auto"
                               Height="Auto"
                               FontWeight="Normal"/>

                        <TextBlock Text="Last seen 10 minutes ago"
                               Foreground="White"
                               FontSize="14"
                               Height="Auto"
                               Opacity="0.5"
                               FontWeight="Normal"/>
                    </StackPanel>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1">
                <ItemsControl Margin="15" ItemsSource="{Binding Messages, UpdateSourceTrigger=PropertyChanged}">
                    <ItemsControl.Resources>
                        <DataTemplate DataType="{x:Type models:MessageModel}">
                            <Border Background="#323232"
                                    Width="Auto"
                                    Height="Auto"
                                    MinWidth="170"
                                    MinHeight="5"
                                    MaxWidth="600"
                                    MaxHeight="800"
                                    CornerRadius="25, 25, 25, 25"
                                    Margin="5"
                                    Padding="2"
                                    HorizontalAlignment="Left">
                                <StackPanel>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Padding="10" Grid.Row="0" Foreground="White" FontSize="13" FontFamily="Poppins" TextWrapping="Wrap" Text="{Binding Content}"/>
                                        <Label Grid.Row="1" Foreground="White" Content="{Binding SendTime}" FontSize="12"  FontFamily="Poppins" FontWeight="Normal" HorizontalAlignment="Right" ContentStringFormat="{}{0:HH:mm}"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.Resources>
                </ItemsControl>

            </ScrollViewer>
          


            <Grid Grid.Row="2" Margin="5" Background="{StaticResource BottomChatPanelColorBrush}" Width="Auto" Height="Auto">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="45"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Width="23"
                        Style="{x:Null}"
                        Background="Transparent"
                        BorderThickness="0"
                        Height="23">
                    <iconPacks:PackIconMaterialDesign     Kind="AttachFile"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"
                                                          Width="23"
                                                          Height="23"
                                                          RotationAngle="220"
                                                          Opacity="1"
                                                          Grid.Column="2"
                                                          SnapsToDevicePixels="True"
                                                          Foreground="White" />
                </Button>


                <Button Grid.Column="3"
                    Width="30"
                    Style="{x:Null}"
                    Height="30"
                    Command="{Binding SendMessageCommand}">
                </Button>


                <TextBox Grid.Column="1" Tag="Write a message..."
                         BorderThickness="0"
                         FontSize="13"
                         HorizontalAlignment="Left"
                         Text="{Binding MessageText, UpdateSourceTrigger=PropertyChanged}"
                         Height="Auto" 
                         Width="Auto"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
