<Page x:Class="Client.Views.Pages.ChatView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views.Pages"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      mc:Ignorable="d" 
      d:DesignHeight="620" d:DesignWidth="1085"
      Title="ChatView">

    <Grid>

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>


            <Border Background="{StaticResource HeaderChatColorBrush}">
                <Grid HorizontalAlignment="Stretch" Margin="0">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <StackPanel  VerticalAlignment="Center" Grid.Column="1">
                        <Label Content="{Binding UserModel.UserName}" 
                               Foreground="White"
                               HorizontalContentAlignment="Left"
                               FontSize="14"
                               Padding="0, 5, 5, 5"
                               Width="Auto"
                               Height="Auto"
                               FontWeight="Normal"/>

                        <TextBlock Text="Last seen 10 minutes ago"
                               Foreground="White"
                               FontSize="14"
                               Height="Auto"
                               Opacity="0.5"
                               FontWeight="Normal"/>
                    </StackPanel>
                </Grid>
            </Border>


            <Grid Grid.Row="2" Background="{StaticResource BottomChatPanelColorBrush}" Width="Auto" Height="Auto">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0"
                        Width="25"
                        Style="{x:Null}"
                        Background="Transparent"
                        BorderThickness="0"
                        Height="25">
                    <iconPacks:PackIconMaterialDesign     Kind="AttachFile"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"
                                                          Width="23"
                                                          Height="23"
                                                          RotationAngle="45"
                                                          Opacity="1"
                                                          Grid.Column="2"
                                                          SnapsToDevicePixels="True"
                                                          Foreground="White" />
                </Button>


                <Button Grid.Column="3"
                    Width="30"
                    Style="{x:Null}"
                    Background="Transparent"
                    BorderThickness="0"
                    Height="30"
                    Command="{Binding SendMessageCommand}">
                    <iconPacks:PackIconMaterialDesign     Kind="Send"
                                                          VerticalAlignment="Center"
                                                          HorizontalAlignment="Center"
                                                          Width="20"
                                                          Opacity="1"
                                                          Grid.Column="2"
                                                          Height="20"
                                                          SnapsToDevicePixels="True"
                                                          Foreground="White" />
                </Button>




                <TextBox Grid.Column="1" Tag="Write a message..."
                         BorderThickness="0"
                         FontSize="15"
                         HorizontalAlignment="Left"
                         Text="{Binding Message, UpdateSourceTrigger=PropertyChanged}"
                         Height="Auto" 
                         Width="Auto"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
