<Page x:Class="Client.Views.Pages.HomeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views.Pages"
      xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
      xmlns:loadingSpinnerControl="clr-namespace:LoadingSpinnerControl;assembly=LoadingSpinnerControl"
      xmlns:modalControl="clr-namespace:ModalControl;assembly=ModalControl"
      mc:Ignorable="d"
      d:DesignHeight="630" 
      d:DesignWidth="1100">

    <Grid>

        <ContentControl Content="{Binding MenuViewModel}" Panel.ZIndex="1"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0"  Background="{StaticResource UsersListBackgroundColorBrush}" >

                <StackPanel Orientation="Vertical" VerticalAlignment="Bottom">

                    <Button Width="48" Height="48"
                            Background="Transparent">

                        <iconPacks:PackIconMaterialDesign     Kind="NotificationsActive"
                                                              Width="18"
                                                              Height="18"
                                                              Foreground="White" />

                    </Button>

                    <Button Width="48" Height="48"
                            Background="Transparent">

                        <iconPacks:PackIconMaterialDesign     Kind="Person"
                                                              Width="18"
                                                              Height="18"
                                                              Foreground="White" />

                    </Button>

                    <Button Width="48" Height="48"
                            VerticalAlignment="Bottom"
                            Background="Transparent">

                        <iconPacks:PackIconMaterialDesign     Kind="Settings"
                                                              Width="18"
                                                              Height="18"
                                                              Foreground="White" />

                    </Button>

                </StackPanel>


            </Grid>

            <Grid
                Background="{StaticResource UsersListBackgroundColorBrush}" 
                Grid.Column="1">

                <Grid.RowDefinitions>
                    <RowDefinition Height="70"/>
                    <RowDefinition />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Width="220"
                             Height="38"
                             Grid.Column="0"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"
                             Background="#313131"
                             BorderThickness="0"
                             Text="{Binding SearchText, 
                    UpdateSourceTrigger=PropertyChanged}"
                             Tag="Search"/>
                </Grid>

                <ListView    Background="Transparent"
                         x:Name="UsersListView"
                         BorderThickness="0"
                         SelectedItem="{Binding SelectedUser}"
                         ItemsSource="{Binding Contacts}"
                         Grid.Row="1">

                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="Transparent"
                                Height="65">

                                <Grid>
                                    <StackPanel Orientation="Horizontal"
                                            Margin="15, 0, 0, 0">

                                        <Border Width="40"  Height="40" CornerRadius="25" Background="White"/>

                                        <StackPanel Margin="5, 5, 0, 0">
                                            <Label Content="{Binding UserName}" 
                                               Foreground="White"   
                                               FontSize="13"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Delete"/>
                        </ContextMenu>
                    </ListView.ContextMenu>
                </ListView>
            </Grid>

            <loadingSpinnerControl:LoadingSpinner
                Grid.Column="2" 
                Color="White"
                Width="50"
                Height="50"
                Diameter="45"
                IsLoading="{Binding IsLoading}"/>

            <Frame Grid.Column="2" Content="{Binding ChatViewModel}"/>

        </Grid>
    </Grid>





</Page>
